export interface TypeErrorMsg {
    errMsg: string;
    errModule: string;
    pass: boolean;
}

export interface Lengthwise {
    length: number;
}

export interface Range {
    min: number;
    max: number;
}
